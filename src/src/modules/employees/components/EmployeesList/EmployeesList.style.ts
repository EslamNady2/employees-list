import styled from "styled-components";
import { colors } from "src/configs/theme";

export const EmployeesListContainer = styled.div`
  border: 2px solid ${colors.mainGrey};
  background-color: ${colors.white};
`;
